mutation CreateAppUser($name: String!, $email: String!, $imageUrl: String, $bio: String) {
    createAppUser(data: { name: $name, email: $email, imageUrl: $imageUrl, bio: $bio }) {
      id,
      name,
      email,
      imageUrl
    }
}