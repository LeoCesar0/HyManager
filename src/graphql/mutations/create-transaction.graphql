mutation CreateTransaction($amount: Int!, $description: String, $bankAccountId: ID!, $color: ColorInput, $date: DateTime!) {
  createTransaction(data: {amount: $amount, description: $description, color: $color, date: $date, bankAccount: {connect: {id: $bankAccountId}}}){
    id,
    amount
  }
}